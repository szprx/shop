spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/shop'
    username: shop
    password: shop
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQL81Dialect
  mail:
    host: smtp.gmail.com
    port: '465'
    username: jan.kowalski.shop.app@gmail.com
    password: blhdlqkfsgeezlsc
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
